# Pandoc から Hugo への移行計画

## 1. 目的

月ごとの Markdown (`log/dYYYYMM.md`) を今後も日記の正本として使いながら、Hugo で日別記事、月別一覧、トップページ、RSS を生成し、GitHub Pages の `https://kaityo256.github.io/log/` で公開する。

編集の流れは次の形を目標とする。

1. 従来どおり、その月の `log/dYYYYMM.md` を編集する。
2. 変換スクリプトが変更された月だけを日別 Markdown に変換する。
3. ローカルでは Hugo の開発サーバーで確認する。
4. `main` への push 時に GitHub Actions が変換、検証、Hugo ビルド、GitHub Pages へのデプロイを行う。

## 2. 現状と移行時の前提

- 原稿は `log/dYYYYMM.md` にあり、月の見出しと日ごとのレベル2見出しから構成されている。
- 日付見出しは `## 2月28日(木)` の形式に統一されている。
- 月冒頭にあった `## TODO` は削除済みで、日付以外のレベル2見出しは原稿の構造として使用しない。
- 同じ日付の見出しが複数ある既存データもあるため、初回変換前に検出し、同日の本文として順序を保って結合するか、原稿を修正するかを決める。変換時に黙って上書きしてはならない。
- `log/images/` の画像、月をまたぐ Markdown リンク、HTML のアンカー、コードブロック、MathJax 記法を維持する必要がある。
- 現在は Makefile から Pandoc を呼び、生成した `docs/` を GitHub Actions で Pages にデプロイしている。
- `log/d202608.md` には作業開始時点で未コミットの変更がある。実装時もこの変更を保存し、上書きしない。

## 3. 採用する構成

Hugo テーマへの強い依存を避け、リポジトリ内の小さな独自レイアウトと CSS で構築する。これにより、`/log/` 配下でのURL、月別表示、既存の素朴な外観、レスポンシブ対応を明示的に管理できる。

想定する主な構成は以下のとおり。

```text
.
├── hugo.toml
├── log/                         # 月別原稿（正本、従来どおり編集）
│   ├── d202608.md
│   └── images/
├── scripts/
│   └── split_diary.py           # 月別原稿から日別コンテンツを生成
├── content/
│   └── diary/
│       └── 2026/08/08/index.md  # 自動生成物
├── layouts/                     # HTML、一覧、RSS のテンプレート
├── assets/css/main.css
├── static/images/               # log/images の公開方法を設定
├── data/
│   └── split-manifest.json      # 原稿ハッシュと生成先の対応
├── tests/                       # 分割処理のテストデータとテスト
├── Makefile
└── .github/workflows/main.yml
```

`content/diary/` と manifest を Git 管理するか、ビルド時生成に限定するかは実装開始時に確定する。再現性と差分レビューを優先するなら Git 管理、リポジトリの重複を避けるならビルド時生成とする。どちらの場合も変換スクリプト自体は月単位の増分更新を保証する。

## 4. URL 設計

- 日別記事: `/log/YYYY/MM/DD/`
- 月別ページ: `/log/YYYY/MM/`
- 年別ページ: `/log/YYYY/`
- トップページ: `/log/`
- RSS: `/log/index.xml`

月別ページにはその月の日記を日付順に表示し、各日への恒久リンクを付ける。「日ごと、月ごとに閲覧可能」という要件を満たすため、月別ページは単なるリンク一覧ではなく、少なくとも日付と本文（または設定で本文全文）を読める形にする。

既存URL `dYYYYMM.html#DD` は外部から参照されている可能性がある。GitHub Pages では任意のサーバーリダイレクトを設定できないため、旧URLと同じ位置に互換HTMLを生成し、新しい月別URLへ誘導する。フラグメントを保った完全な転送が必要なら JavaScript を併用する。実装時に既存URLの維持を受け入れ条件として確認する。

## 5. 月別 Markdown の分割仕様

### 5.1 入力と解析

- 対象はファイル名が `log/dYYYYMM.md` に一致するファイルだけとする。
- ファイル名の年月、先頭の月見出し、各日付見出しが矛盾しないことを検証する。
- 日付見出しは `## M月D日(曜)` の形式を認識する。
- 日付以外のレベル2見出し、実在しない日付、対象月外の日付、解析不能な日付見出しはエラーにし、ファイル名と行番号を表示する。
- 曜日表記は暦から検算する。ただし、過去原稿に曜日の誤記がある場合に本文を勝手に修正しない。初回移行では警告一覧を作り、厳格なエラーにするかを決める。
- コードフェンス内の `##` は見出しとして扱わない Markdown 対応の解析にする。
- 同一日が複数回現れた場合は警告またはエラーにし、採用した方針をテストで固定する。

### 5.2 出力

各日を Hugo のページバンドルとして出力し、最低限次の front matter を付与する。

```yaml
---
title: "2026年8月8日"
date: 2026-08-08T00:00:00+09:00
lastmod: 2026-08-08T00:00:00+09:00
slug: "08"
type: diary
source_month: "d202608.md"
generated: true
---
```

- タイムゾーンは `Asia/Tokyo` とする。
- 本文の順番と Markdown の内容を可能な限りそのまま保持する。
- 月をまたぐ内部リンクは新URLへ変換し、画像リンクは Hugo の `baseURL` が `/log/` 配下でも壊れない形に統一する。
- MathJax、表、引用、コードブロックなど、現行の日記で使われる記法を fixture で検証する。

### 5.3 変更月だけを更新する仕組み

更新判定はファイルの更新時刻だけに依存せず、入力内容の SHA-256 と変換スクリプトの仕様バージョンを `data/split-manifest.json` に保存して行う。

- ハッシュが一致する月は読み飛ばし、その月の日別ファイルには書き込まない。これにより mtime も変えない。
- 新規またはハッシュが変わった月だけを一時ディレクトリへ変換し、検証成功後に対象月の出力へ反映する。
- 既存ファイルと生成内容が同じ場合は置換しない。
- 日付を削除した場合は、変更された月に属する不要な日別ファイルだけを削除する。他の月には触れない。
- 変換失敗時は、その月の既存生成物と manifest を変更しない。
- `--all`（全件再生成）、`--check`（生成物が最新か検証）、`--month YYYY-MM`（明示した月だけ変換）を用意する。
- 変換ロジックや front matter の仕様変更時は仕様バージョンを上げ、必要な全件再生成を明示的に行う。

## 6. Hugo の表示機能

- トップページに最近の日記と年月別アーカイブへの導線を表示する。
- 日別ページに前の日、次の日、属する月へのナビゲーションを表示する。
- 月別ページに対象月の日記を日付順で表示する。
- 年別ページに月別アーカイブを表示する。
- `<meta name="viewport">` とモバイル優先CSSを用い、狭い画面でも本文、画像、表、コードが横にはみ出さないようにする。
- 見出し階層、本文幅、フォーカス表示、色のコントラストを整え、キーボードでも移動できる構造にする。
- Hugo の RSS 出力を設定し、サイトの `<head>` に feed の autodiscovery link を置く。
- RSS には日別記事を新しい順に含め、タイトル、恒久URL、公開日時、本文または要約を出力する。`baseURL` と `languageCode = "ja-jp"` を正しく設定し、絶対URLを生成する。

## 7. ローカル開発

Makefile に少なくとも次の入口を用意し、README に必要な Hugo と Python のバージョン、導入方法、日常の編集手順を記載する。

- `make generate`: 変更月だけを日別コンテンツへ変換
- `make check`: 変換結果、リンク、Hugo 設定を検証
- `make serve`: 変換後に `hugo server` を起動し、`/log/` を含む公開時と同じパスで確認
- `make build`: production 用の `baseURL` で静的サイトを生成
- `make clean`: Hugo の公開物やキャッシュのみを消し、月別原稿と管理対象の生成物は消さない

可能なら Hugo Extended のバージョンを固定し、ローカルとCIで同じバージョンを使う。Python側は標準ライブラリ中心とし、追加パッケージが必要ならバージョンを固定する。

## 8. CI/CD

`.github/workflows/main.yml` を Hugo 用に置き換える。

1. `main` への push と手動実行をトリガーにする。
2. リポジトリを checkout する。
3. 固定バージョンの Python と Hugo Extended をセットアップする。
4. 分割スクリプトを実行する。
5. `--check`、単体テスト、Hugo の production build を実行する。
6. 生成された公開ディレクトリだけを Pages artifact としてアップロードする。
7. GitHub Pages にデプロイする。

Pull Request ではデプロイせず、変換テストと Hugo build だけを行う構成も追加する。Actions には最小権限を設定し、同時デプロイの concurrency を維持する。

## 9. 実装フェーズ

### Phase 1: 調査と仕様固定

- 全月の見出し形式、重複日、曜日不一致、内部リンク、画像、特殊Markdownを機械的に棚卸しする。
- 旧URL互換、重複日の扱い、生成物をGit管理するかを確定する。
- 代表的な月と例外をテスト fixture にする。

### Phase 2: 分割スクリプト

- parser、front matter生成、リンク変換、manifest、原子的更新を実装する。
- 正常系、見出し形式の違い、重複、無効日付、コードフェンス、更新なし、1か月だけ変更、日付削除をテストする。
- 全原稿に対して dry-run し、欠落や意図しない本文変更がないことを確認する。

### Phase 3: Hugo サイト

- 設定、独自レイアウト、アーカイブ、ナビゲーション、CSS、MathJax、RSSを実装する。
- `/log/` のサブパスを指定したローカルビルドでリンクと画像を確認する。
- 旧月別URL用の互換ページを生成する。

### Phase 4: ローカル操作と文書

- Makefile を更新する。
- README をHugo版のセットアップ、編集、確認、トラブルシュート手順に更新する。
- Pandoc固有のファイルや処理は、Hugo版の動作確認後に削除する。

### Phase 5: CI/CD と切り替え

- GitHub Actions を更新し、PRで検証、`main` でデプロイする。
- 初回は生成artifactを確認し、既存サイトとの主要ページ比較を行う。
- Pages公開後、トップ、複数の年月、日別記事、画像、内部リンク、RSSを実URLで確認する。

## 10. テストと受け入れ条件

- 月別原稿は従来のファイル名と記述形式のまま編集できる。
- 変更のない月は変換後も生成ファイルの内容と更新時刻が変わらない。
- 1か月を編集した場合、その月の生成物と manifest の該当情報以外は変わらない。
- 月別原稿の日付と生成された日別記事が過不足なく一対一対応する（採用した重複日方針を除く）。
- 本文、コード、数式、画像、内部・外部リンクが表示できる。
- `/log/YYYY/MM/DD/` と `/log/YYYY/MM/` でそれぞれ日別・月別に閲覧できる。
- 代表的な旧URLから新しい内容へ到達できる。
- スマートフォン幅、タブレット幅、デスクトップ幅で読みやすく、画像とコードがレイアウトを壊さない。
- `/log/index.xml` が有効なRSSとして取得でき、主要RSS readerで購読できる。
- `make serve` だけでローカル確認を開始できる。
- clean checkout から `make build` が成功する。
- `main` への push 後にGitHub Actionsが成功し、GitHub Pagesが更新される。

## 11. 主なリスクと対策

- **過去原稿の表記揺れ**: 正規表現だけで楽観的に処理せず、全件検査と fixture テストを先に行う。
- **生成時の意図しない大量差分**: ハッシュmanifest、内容比較、月単位の一時出力、dry-runで防ぐ。
- **既存リンク切れ**: 旧URL互換ページとリンク検査を用意する。
- **画像パス切れ**: `/log/` 付きの production build をCIで検査する。
- **Hugo更新による差異**: HugoバージョンをローカルとCIで固定する。
- **RSSの日付・URL不正**: `Asia/Tokyo` と production `baseURL` を固定し、XML検証を行う。
- **正本と生成物の混同**: `log/dYYYYMM.md` が正本であり、日別ファイルは直接編集しないことをREADMEと生成ファイル冒頭に明記する。

## 12. 完了後の通常運用

```text
月別 Markdown を編集
        ↓
make serve（変更月だけ分割してプレビュー）
        ↓
commit / push
        ↓
GitHub Actions（変換・検証・Hugo build）
        ↓
GitHub Pages にデプロイ
```

この移行では、初回の全件変換を除き、日常的に編集した月だけが分割処理の更新対象となる。Hugoの公開ディレクトリは毎回再ビルドしてよいが、正本および日別中間データへの書き込み範囲は月単位で限定する。
